[pytest]
testpaths = tests
addopts = -v --tb=short
markers =
    network: tests that require internet access to Google's Speech API
    audio: tests that require espeak-ng or a STT_TEST_WAV file with real speech
